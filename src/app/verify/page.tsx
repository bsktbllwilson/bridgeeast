import { Container } from '@/components/ui/container'

// Stub — full proof-of-funds verification flow comes in a follow-up.
export default function VerifyPage() {
  return (
    <Container className="py-24 md:py-32">
      <div className="mx-auto max-w-md text-center">
        <h1 className="font-display text-4xl text-brand-ink md:text-5xl">Verify Your Account</h1>
        <p className="mt-4 text-brand-muted">
          Proof of funds / SBA pre-qualification flow coming soon.
        </p>
      </div>
    </Container>
  )
}
